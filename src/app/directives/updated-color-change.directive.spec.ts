import { UpdatedColorChangeDirective } from './updated-color-change.directive';

describe('UpdatedColorChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdatedColorChangeDirective();
    expect(directive).toBeTruthy();
  });
});
