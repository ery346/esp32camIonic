import { TimmerStringPipe } from './timmer-string.pipe';

describe('TimmerStringPipe', () => {
  it('create an instance', () => {
    const pipe = new TimmerStringPipe();
    expect(pipe).toBeTruthy();
  });
});
